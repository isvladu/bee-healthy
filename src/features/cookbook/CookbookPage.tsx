import { PagePlaceholder } from '@/components/PagePlaceholder';

export function CookbookPage() {
  return (
    <PagePlaceholder
      title="Cookbook"
      description="Save recommended recipes with macros per serving."
      phase="Phase 4"
    />
  );
}
