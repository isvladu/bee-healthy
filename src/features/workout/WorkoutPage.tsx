import { PagePlaceholder } from '@/components/PagePlaceholder';

export function WorkoutPage() {
  return (
    <PagePlaceholder
      title="Workout"
      description="Import a workout from raw text, track it week by week, and see insights."
      phase="Phase 5–6"
    />
  );
}
